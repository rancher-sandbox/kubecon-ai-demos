apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: display-discover-daemonset
spec:
  selector:
    matchLabels:
      name: display-discover
  template:
    metadata:
      labels:
        name: display-discover
    spec:
      containers:
      - env:
        - name: DISCOVERY_HANDLERS_DIRECTORY
          value: /var/lib/akri
        image: atgracey/display-discover:latest
        imagePullPolicy: IfNotPresent
        name: display-discover
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /var/lib/akri
          name: discovery-handlers
      volumes:
      - hostPath:
          path: /var/lib/akri
        name: discovery-handlers
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: xconfig
data:
  .xinitrc: |-
    #!/bin/bash
    set -x

    xset -dpms
    xset s off
    xset s noblank
    xhost +
    exec firefox --kiosk http://game-client.game:8080
---
apiVersion: akri.sh/v0
kind: Configuration
metadata:
  name: udev-display
spec:
  brokerProperties: {}
  brokerSpec:
    brokerPodSpec:
      containers:
        - name: x11
          args:
            - '-c'
            - |
              startx -- -nocursor
          command:
            - /bin/bash
          env:
            - name: DBUS_SESSION_BUS_ADDRESS
              value: unix:path=/run/user/0/bus
          image: registry.opensuse.org/home/atgracey/wallboardos/15.4/x11:ff
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: true
            privileged: true
            readOnlyRootFilesystem: false
          volumeMounts:
            - mountPath: /run/user
              name: runuser
            - mountPath: /tmp
              name: tmp
            - mountPath: /root
              name: home
            - mountPath: /root/.xinitrc
              name: xconfig
              subPath: .xinitrc
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 5
      volumes:
        - hostPath:
            path: /run/user
            type: Directory
          name: runuser
        - name: tmp
          emptyDir: {}
        - name: home
          emptyDir: {}
        - configMap:
            defaultMode: 0777
            name: xconfig
            items:
              - key: .xinitrc
                path: .xinitrc
          name: xconfig
  capacity: 1
  discoveryHandler:
    name: display
